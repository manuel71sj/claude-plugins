# commit plugin

Conventional Commits skill for Claude Code.

## Local skill replacement

This plugin replaces the local skill at `~/.claude/skills/commit/`.
After installing, remove the local copy:

    rm -rf ~/.claude/skills/commit

## Install

    /plugin install commit@manuel71sj-plugins
